
lista = [3, 1, -1, 0, 4, 10]

def encontra_maior(array):
    if not array:  
        return None
    maior = array[0]
    for i in range(1, len(array)):
        if array[i] > maior:
            maior = array[i]
    return maior

print(encontra_maior(lista))
